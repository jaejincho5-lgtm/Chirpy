# SCORECARD — Project Chirpy, marked harshly

**Written:** 2026-07-05, post P0-1 + prompt-caching + eval-triage · Companion to [`PLAN.md`](./PLAN.md) and [`OPTIMIZATIONS.md`](./OPTIMIZATIONS.md).
Criteria: operational ROI, automation accuracy, cycle-time reduction, upsell/forecasting impact. Graded against
what an ops exec would accept as evidence, not against hackathon vibes.

| Criterion | Grade | One-line verdict |
|---|---|---|
| Operational ROI | **D** | 100% of the cost is real, 0% of the return is |
| Automation accuracy | **C−** | 73% on its own hand-picked 11 cases; everything else unmeasured |
| Cycle time reduction | **F (no evidence)** | Not a single latency number exists in the repo |
| Upsell / forecasting impact | **C+** | One real methodological result standing on synthetic data; forecasting is vaporware |

---

## 1. Operational ROI — D

**Why so harsh:** every VND of claimed value is `simulation × assumption`. OMS mocked, loyalty mocked, payments
mocked, POS history generated by `lib/reco/pos-sim.ts`. Costs are real (gateway spend is measurable to the cent).
No counterfactual is defined (staff handle-time? app funnel conversion?), so ROI isn't just unproven — it's undefined.

**What's real:** unit economics. Post-caching, ~$0.017/conversation on Opus (~$0.003 Haiku) vs a ~99.000₫ basket
→ AI cost ≈ **0.4–1% of basket**. Measured, not assumed.

**Improve:**
- **C9 cost-per-order** (1–2 h): `totalUsage` → `kfc_order_events` → "Chi phí AI đơn này: ~X₫" on `/backend`.
- **C10 `/api/stats`**: deck numbers from real Supabase rows.
- Define the counterfactual in one sentence pre-demo ("a human CSR takes ~4 min per Messenger order; Chirpy takes
  0 human-minutes") and openly gate the revenue side on a pilot. Judges punish fake ROI harder than absent ROI.

## 2. Automation accuracy — C−

**Why so harsh:** the only clean number is 8/11 on a self-authored test set (pre-triage). The full 41-case baseline
has never completed. The single real-world transcript produced 5 failure modes in 3 turns (now pinned, n=2 replays).
Zero real users. Teencode robustness (E14) untested. Adversarial (E13) untested. The channel path silently duplicated
carts until 2026-07-05 and no eval caught it, because no eval exercises the channel path. Coverage systematically
avoids where the system actually breaks.

**Triage state (2026-07-05):** `agent-budget-craving` fixed (over-strict case). `agent-oos-recovery` +
`agent-craving-spicy` have harness/prompt fixes in place but are Opus-verification-gated — Haiku does not follow
multi-step discipline reliably and is not the demo model.

**Improve:**
- Run the one honest **Opus baseline** (41 cases, ~$0.60–0.80 cached) before demo day; record it dated in README.
- **E13 adversarial block** (2–3 h) — doubles as the security slide.
- **E14 teencode set** (2 h) — market-specific, honest number for the deck.
- Add a **channel-path eval** (webhook simulator driven) — the duplicate-cart class of bug is invisible to Suite 2.
- Post-P0: mass persona simulation (N customers × completion rate) on Haiku — the accuracy metric that means something.

## 3. Cycle time reduction — F for evidence

**Why so harsh:** B6 never built → no p50/p95, no TTFT, nothing. An Opus 8-step chain plausibly runs 8–20 s/turn and
the Messenger user gets silence (B7 typing indicator unbuilt, B8 watchdog unbuilt). Honest comparison class: a
*returning* customer's "như mọi khi? → ok" plausibly beats 3 minutes of app-tapping; a *new* customer's 6-turn
conversation is probably slower than the app. None of this can be claimed without numbers.

**Improve:**
- **B6 latency budget in Suite 2** (1 h): p50/p95 + slowest case in the eval footer, free on every run.
- **B7 typing indicator** (30 min) + **B8 6 s interim watchdog** (1 h): felt-latency insurance for the phone demo.
- Prompt caching landed 2026-07-05 (system+tools cached; reads ≈ 0.1× input price, TTFT drops from turn 2) — B6 quantifies it.
- Track **turns-to-completed-order, return vs new** — memory shortening the conversation is the real cycle-time story.

## 4. Upsell / forecasting impact — C+

**Why so harsh (upsell):** +9.6pp precision@1 on 965 held-out events is real methodology on synthetic personas buying
from a synthetic POS. The AOV projection multiplies that by assumed take-rates (12–40%) — the assumption does all the
work in the headline. Accept/decline events persist (`kfc_suggestion_events`) but no live take-rate is computed or
shown anywhere (A4 unbuilt). The deterministic combo optimizer is the honest star (unit-pinned 14.000₫/33.000₫,
voucher survives the swap).

**Why so harsh (forecasting):** there is none. No demand forecasting, no reorder-gap prediction in production; the
proactive nudge (P2-8) — the only forecasting-shaped feature — is unbuilt. The demo clock simulates time, it doesn't
predict anything. Saying "forecasting" in the pitch today is overclaiming.

**Improve:**
- **A1 live profile card + A4 take-rate counter** (~3 h): live on-stage number — "Gợi ý: 3/4 được nhận · +44.000₫".
- **A3 seed Mẹ Linh** (15 min): per-customer memory, not one hardcoded demo user.
- **P2-8 nudge** + precision eval on held-out sim customers — the only legitimate path to the word "forecasting".
- Deck reframe: "measured +9.6pp on held-out simulation; take-rate is the pilot KPI, instrumentation already live."

---

## The meta-problem and the cheapest fix

Strengths (optimizer, taste memory, channel adapter) are real engineering; claims (ROI, speed, revenue) are all
simulation-shaped. The fastest grade improvement is not features — it is **instrumentation that converts existing
behavior into measured numbers**: **C9 + B6 + A4 ≈ half a day** and moves three of the four grades.

| Half-day sprint | Grade moved |
|---|---|
| C9 cost-per-order line | ROI D → C |
| B6 latency p50/p95 | Cycle time F → C (or an honest D — but measured) |
| A4 live take-rate counter | Upsell C+ → B− |
| (Opus baseline, when approved) | Accuracy C− → whatever the truth is |
